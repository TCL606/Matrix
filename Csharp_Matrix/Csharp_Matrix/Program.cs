using System;
using TCL_Matrix;

namespace Program
{
    public class Program
    {
        static void Main()
        {
            //var A = Matrix.ReadFromFile("C:\\Users\\admin\\Desktop\\test.txt");
            //Matrix A = Matrix.RandomMatrix(5, 4);
            //A?.Display();
            //Matrix? Q, R;
            //Matrix? U, Sigma, VT;
            //if (A.QR(out Q, out R))
            //{
            //    Q?.Display();
            //    R?.Display();
            //    (Q.Transpose()*Q).Display();
            //    (Q * R).Display();
            //}
            //if(A.SVD(out U, out Sigma, out VT))
            //{
            //    (U.Transpose()*U).Display();
            //    Sigma?.Display();
            //    (VT * VT.Transpose()).Display();
            //}
        }
    }
}